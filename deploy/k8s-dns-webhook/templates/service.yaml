apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-dns-webhook.fullname" . }}
  labels:
    app: {{ include "k8s-dns-webhook.name" . }}
    chart: {{ include "k8s-dns-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "k8s-dns-webhook.name" . }}
    release: {{ .Release.Name }}
